import { SortAndLimitPipePipe } from './sort-and-limit-pipe.pipe';

describe('SortAndLimitPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SortAndLimitPipePipe();
    expect(pipe).toBeTruthy();
  });
});
